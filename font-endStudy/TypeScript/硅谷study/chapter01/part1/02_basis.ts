/* let a: number;

a = 10;
a = 33;
// 报错因为a的类型是number，不能赋值String
// a = '123'

// 声明变量直接赋值
let b: boolean = false

// 自动判断，后续为boolean，不能再改变
let c = false

function sum(x:number,y:number): number{
    return x + y
}
console.log(sum(1,8));
let result = sum(123,456) //result也为number类型
 */